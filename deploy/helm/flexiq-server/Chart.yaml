apiVersion: v2
name: flexiq-server
description: The FlexiQ scheduler, dashboard, and executor sidecar injector
type: application
# Chart version and appVersion move together: the chart only ever ships the
# image built from the same commit, and `node scripts/version.mjs --check`
# enforces that in CI.
version: 2.0.0
appVersion: "2.0.0"
home: https://flexiq.grigori.in
sources:
  - https://github.com/ByteVeda/flexiq
keywords:
  - task-queue
  - scheduler
  - jobs
maintainers:
  - name: ByteVeda
    url: https://github.com/ByteVeda
annotations:
  artifacthub.io/license: MIT
