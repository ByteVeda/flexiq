apiVersion: v2
name: taskito-server
description: The Taskito scheduler, dashboard, and executor sidecar injector
type: application
# Chart version and appVersion move together: the chart only ever ships the
# image built from the same commit, and `node scripts/version.mjs --check`
# enforces that in CI.
version: 0.23.0
appVersion: "0.23.0"
home: https://taskito.grigori.in
sources:
  - https://github.com/ByteVeda/taskito
keywords:
  - task-queue
  - scheduler
  - jobs
maintainers:
  - name: ByteVeda
    url: https://github.com/ByteVeda
annotations:
  artifacthub.io/license: MIT
