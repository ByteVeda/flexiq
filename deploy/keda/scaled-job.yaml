# KEDA ScaledJob for ephemeral batch workers.
#
# Unlike ScaledObject (which scales a long-running Deployment),
# ScaledJob creates short-lived Kubernetes Jobs — one pod per N
# pending tasks. Use this for batch/ETL workloads.
#
# Prerequisites:
#   1. Deploy `flexiq scaler --app myapp:queue --port 9091` as a Service
#   2. Install KEDA
apiVersion: keda.sh/v1alpha1
kind: ScaledJob
metadata:
  name: flexiq-batch-worker
  namespace: default
spec:
  jobTargetRef:
    template:
      spec:
        containers:
          - name: flexiq-worker
            image: your-image:latest
            command: ["flexiq", "worker", "--app", "myapp:queue"]
        restartPolicy: Never
  pollingInterval: 15
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 5
  maxReplicaCount: 20
  scalingStrategy:
    strategy: default             # or "accurate" for exact 1:1 mapping
  triggers:
    - type: metrics-api
      metadata:
        url: "http://flexiq-scaler:9091/api/scaler"
        valueLocation: "metricValue"
        targetValue: "5"          # one pod per 5 pending jobs
