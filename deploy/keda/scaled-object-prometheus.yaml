# KEDA ScaledObject using Prometheus as the metric source.
#
# Prerequisites:
#   1. Prometheus scraping flexiq's /metrics endpoint
#   2. PrometheusStatsCollector running with the worker
#
# Adjust serverAddress, query, and threshold to match your setup.
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: flexiq-worker-prometheus
  namespace: default
spec:
  scaleTargetRef:
    name: flexiq-worker          # Deployment name
  pollingInterval: 15
  cooldownPeriod: 60
  minReplicaCount: 0
  maxReplicaCount: 10
  triggers:
    # Scale based on queue depth
    - type: prometheus
      metadata:
        serverAddress: "http://prometheus:9090"
        metricName: flexiq_queue_depth
        query: sum(flexiq_queue_depth{queue="default"})
        threshold: "10"
    # Scale based on worker utilization
    - type: prometheus
      metadata:
        serverAddress: "http://prometheus:9090"
        metricName: flexiq_worker_utilization
        query: flexiq_worker_utilization{queue="default"}
        threshold: "0.8"
