/**
 * Standalone liveness and readiness probes over a {@link org.byteveda.flexiq.FlexiQ}
 * handle. Wire them into any HTTP framework, a CLI check, or a container probe —
 * the built-in dashboard is just another caller.
 */
@NullMarked
package org.byteveda.flexiq.health;

import org.jspecify.annotations.NullMarked;
