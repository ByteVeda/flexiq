---
title: Records & context
description: "Every records & context symbol the Java SDK declares, with its exact signature."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/java/src/main/java/org/byteveda/flexiq/*.java`.

## JobContext

### `ctx.current()`

```java
JobContext current()
```

### `ctx.enter()`

```java
void enter(JobContext context)
```

### `ctx.exit()`

```java
void exit()
```

### `ctx.jobId()`

```java
String jobId()
```

### `ctx.log()`

```java
void log(String message)
void log(TaskLogLevel level, String message, @Nullable String extra)
void log(TaskLogLevel level, String message)
```

### `ctx.publish()`

```java
void publish(Object value)
```

### `ctx.setProgress()`

```java
void setProgress(int progress)
```

### `ctx.step()`

```java
StepContext step()
```

### `ctx.taskName()`

```java
String taskName()
```

## JobContext.Sink

### `sink.setProgress()`

```java
void setProgress(String jobId, int progress)
```

### `sink.writeTaskLog()`

```java
void writeTaskLog(
    String jobId,
    String taskName,
    String level,
    String message,
    @Nullable String extra
)
```

