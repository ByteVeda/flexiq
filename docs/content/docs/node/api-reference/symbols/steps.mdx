---
title: Durable step signatures
description: "The durable step signatures the Node.js SDK declares, straight from its own declarations."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/node/native/index.d.ts`.

## JsStepSession

### `session.beginRun()`

```typescript
session.beginRun(
    name: string,
    key?: string | undefined | null
): Promise<JsStepDecision>
```

### `session.commitRun()`

```typescript
session.commitRun(encoded: Buffer): Promise<void>
```

### `session.finish()`

```typescript
session.finish(): void
```

### `session.runKey()`

```typescript
session.runKey(): Promise<string>
```

### `session.sleepFor()`

```typescript
session.sleepFor(
    durationMs: number,
    name?: string | undefined | null,
    key?: string | undefined | null
): Promise<JsStepSleepOutcome>
```

### `session.sleepUntil()`

```typescript
session.sleepUntil(
    wakeAt: number,
    name?: string | undefined | null,
    key?: string | undefined | null
): Promise<JsStepSleepOutcome>
```

