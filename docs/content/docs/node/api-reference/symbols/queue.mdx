---
title: Queue client
description: "Every queue client symbol the Node.js SDK declares, with its exact signature."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/node/native/index.d.ts`.

## module

### `reservedSettingPrefixes()`

```typescript
reservedSettingPrefixes(): Array<string>
```

## JsQueue

### `queue.ackMessage()`

```typescript
queue.ackMessage(
    topic: string,
    subscriptionName: string,
    messageId: string
): Promise<boolean>
```

### `queue.ackTopicCursor()`

```typescript
queue.ackTopicCursor(
    topic: string,
    subscriptionName: string,
    cursor: string
): Promise<boolean>
```

### `queue.acquireLock()`

```typescript
queue.acquireLock(name: string, ownerId: string, ttlMs: number): boolean
```

### `queue.cancelJob()`

```typescript
queue.cancelJob(id: string): boolean
```

### `queue.cascadeSkipPending()`

```typescript
queue.cascadeSkipPending(runId: string): void
```

### `queue.checkFanOutCompletion()`

```typescript
queue.checkFanOutCompletion(
    runId: string,
    parentNodeName: string
): JsFanOutCompletion | null
```

### `queue.compensationNodeForJob()`

```typescript
queue.compensationNodeForJob(jobId: string): JsWorkflowNodeRef | null
```

### `queue.countPendingByQueue()`

```typescript
queue.countPendingByQueue(queue: string): number
```

### `queue.createDeferredJob()`

```typescript
queue.createDeferredJob(
    runId: string,
    nodeName: string,
    payload: Buffer,
    taskName: string,
    queue: string,
    maxRetries: number,
    timeoutMs: number,
    priority: number
): string
```

### `queue.deadLetters()`

```typescript
queue.deadLetters(
    limit?: number | undefined | null,
    offset?: number | undefined | null
): Promise<Array<JsDeadJob>>
```

### `queue.deadLettersAfter()`

```typescript
queue.deadLettersAfter(
    limit?: number | undefined | null,
    after?: string | undefined | null
): Promise<JsDeadJobPage>
```

### `queue.deadLettersByTask()`

```typescript
queue.deadLettersByTask(
    taskName: string,
    limit?: number | undefined | null,
    offset?: number | undefined | null
): Promise<Array<JsDeadJob>>
```

### `queue.declareTopic()`

```typescript
queue.declareTopic(
    name: string,
    mode: string,
    retentionMs?: number | undefined | null
): Promise<void>
```

### `queue.deleteDead()`

```typescript
queue.deleteDead(deadId: string): boolean
```

### `queue.deletePeriodic()`

```typescript
queue.deletePeriodic(name: string): boolean
```

### `queue.deleteSetting()`

```typescript
queue.deleteSetting(key: string): boolean
```

### `queue.dryRunRetention()`

```typescript
queue.dryRunRetention(
    retention?: RetentionInput | undefined | null
): Promise<string>
```

### `queue.effectiveRetention()`

```typescript
queue.effectiveRetention(): string | null
```

### `queue.enqueue()`

```typescript
queue.enqueue(
    taskName: string,
    payload: Buffer,
    options?: EnqueueOptions | undefined | null
): string
```

### `queue.enqueueCompensation()`

```typescript
queue.enqueueCompensation(
    runId: string,
    nodeName: string,
    payload: Buffer,
    taskName: string,
    queue: string,
    maxRetries: number,
    timeoutMs: number,
    priority: number
): string
```

### `queue.enqueueMany()`

```typescript
queue.enqueueMany(
    taskName: string,
    jobs: Array<EnqueueJob>
): Array<string>
```

### `queue.expandFanOut()`

```typescript
queue.expandFanOut(
    runId: string,
    parentNodeName: string,
    childNames: Array<string>,
    childPayloads: Array<Buffer>,
    taskName: string,
    queue: string,
    maxRetries: number,
    timeoutMs: number,
    priority: number
): Array<string>
```

### `queue.extendLock()`

```typescript
queue.extendLock(name: string, ownerId: string, ttlMs: number): boolean
```

### `queue.finalizeRunIfTerminal()`

```typescript
queue.finalizeRunIfTerminal(runId: string): string | null
```

### `queue.getJob()`

```typescript
queue.getJob(id: string): JsJob | null
```

### `queue.getJobErrors()`

```typescript
queue.getJobErrors(jobId: string): Promise<Array<JsJobError>>
```

### `queue.getLockInfo()`

```typescript
queue.getLockInfo(name: string): JsLockInfo | null
```

### `queue.getMetrics()`

```typescript
queue.getMetrics(
    task: string | undefined | null,
    sinceMs: number
): Promise<Array<JsMetric>>
```

### `queue.getReplayHistory()`

```typescript
queue.getReplayHistory(jobId: string): Promise<Array<JsReplayEntry>>
```

### `queue.getSetting()`

```typescript
queue.getSetting(key: string): string | null
```

### `queue.getTaskLogs()`

```typescript
queue.getTaskLogs(jobId: string): Array<JsTaskLog>
```

### `queue.getTaskLogsAfter()`

```typescript
queue.getTaskLogsAfter(
    jobId: string,
    afterId?: string | undefined | null
): Array<JsTaskLog>
```

### `queue.getWorkflowChildren()`

```typescript
queue.getWorkflowChildren(runId: string): Array<JsWorkflowRun>
```

### `queue.getWorkflowDag()`

```typescript
queue.getWorkflowDag(runId: string): string | null
```

### `queue.getWorkflowNodes()`

```typescript
queue.getWorkflowNodes(runId: string): Array<JsWorkflowNode>
```

### `queue.getWorkflowRun()`

```typescript
queue.getWorkflowRun(runId: string): JsWorkflowRun | null
```

### `queue.getWorkflowRunPlan()`

```typescript
queue.getWorkflowRunPlan(runId: string): JsWorkflowRunPlan | null
```

### `queue.isCancelRequested()`

```typescript
queue.isCancelRequested(id: string): boolean
```

### `queue.jobDag()`

```typescript
queue.jobDag(jobId: string): Promise<JsJobDag>
```

### `queue.leaseTopicMessages()`

```typescript
queue.leaseTopicMessages(
    topic: string,
    subscriptionName: string,
    limit: number,
    visibilityMs: number
): Promise<Array<JsTopicMessage>>
```

### `queue.listArchived()`

```typescript
queue.listArchived(
    limit?: number | undefined | null,
    offset?: number | undefined | null
): Promise<Array<JsJob>>
```

### `queue.listArchivedAfter()`

```typescript
queue.listArchivedAfter(
    limit?: number | undefined | null,
    after?: string | undefined | null
): Promise<JsJobPage>
```

### `queue.listCircuitBreakers()`

```typescript
queue.listCircuitBreakers(): Promise<Array<JsCircuitBreaker>>
```

### `queue.listDeclaredTopics()`

```typescript
queue.listDeclaredTopics(): Promise<Array<JsTopic>>
```

### `queue.listJobs()`

```typescript
queue.listJobs(
    filter?: JobFilter | undefined | null
): Promise<Array<JsJob>>
```

### `queue.listJobsAfter()`

```typescript
queue.listJobsAfter(
    filter?: JobFilter | undefined | null,
    after?: string | undefined | null
): Promise<JsJobPage>
```

### `queue.listJobsFiltered()`

```typescript
queue.listJobsFiltered(
    filter?: DetailedJobFilter | undefined | null
): Promise<Array<JsJob>>
```

### `queue.listJobsFilteredAfter()`

```typescript
queue.listJobsFilteredAfter(
    filter?: DetailedJobFilter | undefined | null,
    after?: string | undefined | null
): Promise<JsJobPage>
```

### `queue.listPausedQueues()`

```typescript
queue.listPausedQueues(): Array<string>
```

### `queue.listPeriodic()`

```typescript
queue.listPeriodic(): Array<JsPeriodicTask>
```

### `queue.listSettings()`

```typescript
queue.listSettings(): Record<string, string>
```

### `queue.listSubscriptions()`

```typescript
queue.listSubscriptions(
    topic?: string | undefined | null
): Promise<Array<JsSubscription>>
```

### `queue.listWorkers()`

```typescript
queue.listWorkers(): Promise<Array<JsWorkerRow>>
```

### `queue.listWorkflowRuns()`

```typescript
queue.listWorkflowRuns(
    definitionName?: string | undefined | null,
    state?: string | undefined | null,
    limit?: number | undefined | null,
    offset?: number | undefined | null
): Array<JsWorkflowRun>
```

### `queue.markWorkflowNodeResult()`

```typescript
queue.markWorkflowNodeResult(
    jobId: string,
    succeeded: boolean,
    error?: string | undefined | null,
    skipCascade?: boolean | undefined | null
): JsWorkflowAdvance | null
```

### `queue.migrate()`

```typescript
queue.migrate(): Promise<MigrationSummary>
```

### `queue.minContract()`

```typescript
queue.minContract(): number
```

### `queue.nackMessage()`

```typescript
queue.nackMessage(
    topic: string,
    subscriptionName: string,
    messageId: string
): Promise<boolean>
```

### `JsQueue.open()`

```typescript
JsQueue.open(options: OpenOptions): JsQueue
```

### `queue.pauseQueue()`

```typescript
queue.pauseQueue(queue: string): void
```

### `queue.publish()`

```typescript
queue.publish(
    topic: string,
    payload: Buffer,
    options?: PublishOptions | undefined | null
): Promise<Array<JsJob>>
```

### `queue.purgeCompleted()`

```typescript
queue.purgeCompleted(olderThanMs: number): Promise<number>
```

### `queue.purgeDead()`

```typescript
queue.purgeDead(olderThanMs: number): Promise<number>
```

### `queue.purgeDeadByTask()`

```typescript
queue.purgeDeadByTask(taskName: string): Promise<number>
```

### `queue.queryTaskLogs()`

```typescript
queue.queryTaskLogs(
    taskName: string | undefined | null,
    level: string | undefined | null,
    sinceMs: number,
    limit: number
): Promise<Array<JsTaskLog>>
```

### `queue.readTopicMessages()`

```typescript
queue.readTopicMessages(
    topic: string,
    subscriptionName: string,
    limit: number
): Promise<Array<JsTopicMessage>>
```

### `queue.reapEphemeralSubscriptions()`

```typescript
queue.reapEphemeralSubscriptions(
    workerId?: string | undefined | null
): Promise<number>
```

### `queue.registerPeriodic()`

```typescript
queue.registerPeriodic(
    name: string,
    taskName: string,
    cronExpr: string,
    args?: Buffer | undefined | null,
    queue?: string | undefined | null,
    timezone?: string | undefined | null,
    enabled?: boolean | undefined | null
): number
```

### `queue.registerSubscription()`

```typescript
queue.registerSubscription(
    topic: string,
    subscriptionName: string,
    taskName: string,
    queue: string,
    durable: boolean,
    ownerWorkerId?: string | undefined | null,
    priority?: number | undefined | null,
    maxRetries?: number | undefined | null,
    timeoutMs?: number | undefined | null,
    mode?: string | undefined | null
): Promise<void>
```

### `queue.releaseLock()`

```typescript
queue.releaseLock(name: string, ownerId: string): boolean
```

### `queue.replayJob()`

```typescript
queue.replayJob(jobId: string): Promise<string>
```

### `queue.requestCancel()`

```typescript
queue.requestCancel(id: string): boolean
```

### `queue.requeueJob()`

```typescript
queue.requeueJob(jobId: string): boolean
```

### `queue.resolveWorkflowGate()`

```typescript
queue.resolveWorkflowGate(
    runId: string,
    nodeName: string,
    approved: boolean,
    error?: string | undefined | null
): void
```

### `queue.resumeQueue()`

```typescript
queue.resumeQueue(queue: string): void
```

### `queue.retryDead()`

```typescript
queue.retryDead(deadId: string): string
```

### `queue.runWorker()`

```typescript
queue.runWorker(
    callback: ((arg: JsTaskInvocation) => Promise<JsTaskOutcome>),
    outcomeCallback: ((arg: JsOutcome) => unknown),
    options?: WorkerOptions | undefined | null
): JsWorker
```

### `queue.setMinContract()`

```typescript
queue.setMinContract(level: number): void
```

### `queue.setPeriodicEnabled()`

```typescript
queue.setPeriodicEnabled(name: string, enabled: boolean): boolean
```

### `queue.setSetting()`

```typescript
queue.setSetting(key: string, value: string): void
```

### `queue.setSettingIf()`

```typescript
queue.setSettingIf(
    key: string,
    expected: string | undefined | null,
    value: string
): boolean
```

### `queue.setSubscriptionActive()`

```typescript
queue.setSubscriptionActive(
    topic: string,
    subscriptionName: string,
    active: boolean
): Promise<boolean>
```

### `queue.setWorkflowNodeCompensated()`

```typescript
queue.setWorkflowNodeCompensated(
    runId: string,
    nodeName: string,
    completedAt: number
): void
```

### `queue.setWorkflowNodeCompensationFailed()`

```typescript
queue.setWorkflowNodeCompensationFailed(
    runId: string,
    nodeName: string,
    error: string,
    completedAt: number
): void
```

### `queue.setWorkflowNodeRunning()`

```typescript
queue.setWorkflowNodeRunning(runId: string, nodeName: string): void
```

### `queue.setWorkflowNodeWaitingApproval()`

```typescript
queue.setWorkflowNodeWaitingApproval(
    runId: string,
    nodeName: string
): void
```

### `queue.setWorkflowRunState()`

```typescript
queue.setWorkflowRunState(
    runId: string,
    state: string,
    error?: string | undefined | null,
    completedAt?: number | undefined | null
): void
```

### `queue.skipWorkflowNode()`

```typescript
queue.skipWorkflowNode(runId: string, nodeName: string): void
```

### `queue.stats()`

```typescript
queue.stats(): Promise<JsStats>
```

### `queue.statsAllQueues()`

```typescript
queue.statsAllQueues(): Promise<Record<string, JsStats>>
```

### `queue.statsByQueue()`

```typescript
queue.statsByQueue(queue: string): Promise<JsStats>
```

### `queue.submitWorkflow()`

```typescript
queue.submitWorkflow(
    name: string,
    version: number,
    dagBytes: Buffer,
    stepMetadataJson: string,
    nodePayloads: Record<string, Buffer>,
    queueDefault?: string | undefined | null,
    paramsJson?: string | undefined | null,
    deferredNodeNames?: Array<string> | undefined | null,
    parentRunId?: string | undefined | null,
    parentNodeName?: string | undefined | null
): string
```

### `queue.supportsSteps()`

```typescript
queue.supportsSteps(): boolean
```

### `queue.topicBacklogStats()`

```typescript
queue.topicBacklogStats(): Promise<Array<JsTopicStat>>
```

### `queue.topicLogStats()`

```typescript
queue.topicLogStats(): Promise<Array<JsTopicLogStat>>
```

### `queue.unsubscribe()`

```typescript
queue.unsubscribe(
    topic: string,
    subscriptionName: string
): Promise<boolean>
```

### `queue.updateProgress()`

```typescript
queue.updateProgress(id: string, progress: number): void
```

### `queue.workerHeartbeat()`

```typescript
queue.workerHeartbeat(
    workerId: string,
    resourceHealth?: string | undefined | null
): Promise<Array<string>>
```

### `queue.workflowNodeForJob()`

```typescript
queue.workflowNodeForJob(jobId: string): JsWorkflowNodeRef | null
```

### `queue.writeTaskLog()`

```typescript
queue.writeTaskLog(
    jobId: string,
    taskName: string,
    level: string,
    message: string,
    extra?: string | undefined | null
): void
```

