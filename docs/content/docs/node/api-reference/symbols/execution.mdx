---
title: Workers & executors
description: "Every workers & executors symbol the Node.js SDK declares, with its exact signature."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/node/native/index.d.ts`.

## module

### `startExecutor()`

```typescript
startExecutor(
    callback: ((arg: JsTaskInvocation) => Promise<JsTaskOutcome>),
    options: ExecutorOptions
): Promise<JsExecutor>
```

## JsExecutor

### `executor.disabledMiddleware()`

```typescript
executor.disabledMiddleware(jobId: string): Array<string>
```

### `executor.executorId`

```typescript
executor.executorId: string
```

### `executor.isCancelRequested()`

```typescript
executor.isCancelRequested(jobId: string): boolean
```

### `executor.isRunning()`

```typescript
executor.isRunning(): boolean
```

### `executor.openStepSession()`

```typescript
executor.openStepSession(
    jobId: string,
    attempt: number
): Promise<JsStepSession>
```

### `executor.peer`

```typescript
executor.peer: string
```

### `executor.reportProgress()`

```typescript
executor.reportProgress(jobId: string, progress: number): void
```

### `executor.schedulerId`

```typescript
executor.schedulerId: string
```

### `executor.shutdown()`

```typescript
executor.shutdown(): Promise<void>
```

### `executor.stop()`

```typescript
executor.stop(): void
```

### `executor.supportsSideChannel()`

```typescript
executor.supportsSideChannel(): boolean
```

### `executor.supportsSteps()`

```typescript
executor.supportsSteps(): boolean
```

### `executor.wait()`

```typescript
executor.wait(): Promise<void>
```

### `executor.writeTaskLog()`

```typescript
executor.writeTaskLog(
    jobId: string,
    taskName: string,
    level: string,
    message: string,
    extra?: string | undefined | null
): void
```

## JsWorker

### `worker.id`

```typescript
worker.id: string
```

### `worker.openStepSession()`

```typescript
worker.openStepSession(
    jobId: string,
    attempt: number
): Promise<JsStepSession>
```

### `worker.stop()`

```typescript
worker.stop(): void
```

