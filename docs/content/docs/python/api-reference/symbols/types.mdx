---
title: Records & context
description: "Every records & context symbol the Python SDK declares, with its exact signature."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/python/flexiq/_flexiq.pyi`.

## PyJob

### `job.payload_bytes`

```python
job.payload_bytes: bytes
```

### `job.result_bytes`

```python
job.result_bytes: bytes | None
```

### `job.status`

```python
job.status: str
```

## PyTaskConfig

### `PyTaskConfig()`

```python
PyTaskConfig(
    name: str,
    max_retries: int = 3,
    retry_backoff: float = 1.0,
    timeout: int = 300,
    priority: int = 0,
    rate_limit: str | None = None,
    queue: str = "default",
    circuit_breaker_threshold: int | None = None,
    circuit_breaker_window: int | None = None,
    circuit_breaker_cooldown: int | None = None,
    retry_delays: list[float] | None = None,
    max_retry_delay: int | None = None,
    max_concurrent: int | None = None,
    circuit_breaker_half_open_probes: int | None = None,
    circuit_breaker_half_open_success_rate: float | None = None,
    max_in_flight_per_task: int | None = None,
    retry_budget: str | None = None,
    on_excess: str | None = None
)
```

