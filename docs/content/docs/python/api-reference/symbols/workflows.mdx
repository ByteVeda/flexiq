---
title: Workflow signatures
description: "The workflow signatures the Python SDK declares, straight from its own declarations."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/python/flexiq/_flexiq.pyi`.

## PyWorkflowBuilder

### `PyWorkflowBuilder()`

```python
PyWorkflowBuilder()
```

### `builder.add_step()`

```python
builder.add_step(
    name: str,
    task_name: str,
    after: list[str] | None = None,
    queue: str | None = None,
    max_retries: int | None = None,
    timeout_ms: int | None = None,
    priority: int | None = None,
    args_template: str | None = None,
    kwargs_template: str | None = None,
    fan_out: str | None = None,
    fan_in: str | None = None,
    condition: str | None = None
) -> None
```

### `builder.serialize()`

```python
builder.serialize() -> tuple[bytes, str]
```

### `builder.step_count()`

```python
builder.step_count() -> int
```

### `builder.step_names()`

```python
builder.step_names() -> list[str]
```

## PyWorkflowRunStatus

### `status.node_statuses()`

```python
status.node_statuses() -> dict[str, dict[str, Any]]
```

