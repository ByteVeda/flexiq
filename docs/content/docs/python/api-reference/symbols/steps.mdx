---
title: Durable step signatures
description: "The durable step signatures the Python SDK declares, straight from its own declarations."
---

{/* GENERATED by scripts/api/sync.mjs — run `pnpm sync:api`. Edit the SDK's declarations, not this block. */}

Signatures, parameters, defaults and return types come straight from the
declarations, so they cannot drift from the code. Prose, examples and gotchas
live on the hand-written pages this section sits beside.

Generated from `sdks/python/flexiq/_flexiq.pyi`.

## AttachedSteps

### `AttachedSteps()`

```python
AttachedSteps(
    relay: Any,
    job_frame: str,
    snapshot: bytes,
    supported: bool
)
```

### `steps.open_step_session()`

```python
steps.open_step_session(job_id: str, attempt: int) -> StepSession
```

## StepDecision

### `decision.idempotency_key`

```python
decision.idempotency_key: str
```

### `decision.memoized`

```python
decision.memoized: bytes | None
```

### `decision.step_key`

```python
decision.step_key: str
```

## StepSession

### `session.begin_run()`

```python
session.begin_run(name: str, key: str | None = None) -> StepDecision
```

### `session.commit_run()`

```python
session.commit_run(decision: StepDecision, encoded: bytes) -> None
```

### `session.finish()`

```python
session.finish() -> None
```

### `session.idempotency_key()`

```python
session.idempotency_key(step_key: str) -> str
```

### `session.run_key()`

```python
session.run_key() -> str
```

### `session.sleep_for()`

```python
session.sleep_for(
    duration_ms: int,
    name: str | None = None,
    key: str | None = None
) -> StepSleepOutcome
```

### `session.sleep_until()`

```python
session.sleep_until(
    wake_at: int,
    name: str | None = None,
    key: str | None = None
) -> StepSleepOutcome
```

## StepSleepOutcome

### `outcome.elapsed`

```python
outcome.elapsed: bool
```

### `outcome.step_key`

```python
outcome.step_key: str
```

### `outcome.wake_at`

```python
outcome.wake_at: int
```

## WorkerSteps

### `steps.open_step_session()`

```python
steps.open_step_session(job_id: str, attempt: int) -> StepSession
```

### `steps.owner`

```python
steps.owner: str
```

